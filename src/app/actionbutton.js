const ActionButton = (props) => {
    return (
        <div className='flex justify-center align-middle w-20 h-12 border-2 border-solid border-black m-0.5'>{props.letter}</div>
    );
};

export default ActionButton;